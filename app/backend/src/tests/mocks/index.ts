import allTeams from './teams';
import user from './user';
import allMatches from './matches';
import leaderBoard from './leaderboard';

export default {
  allTeams,
  user,
  allMatches,
  leaderBoard,
};

export { allTeams, user, allMatches, leaderBoard };
